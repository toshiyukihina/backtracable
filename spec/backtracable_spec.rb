require 'spec_helper'

describe Backtracable do
  it 'has a version number' do
    expect(Backtracable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
